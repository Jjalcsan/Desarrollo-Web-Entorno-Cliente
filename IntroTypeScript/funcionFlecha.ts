// Convertir esta funcion a una funcion de flecha
//function resultadoDoble( a, b ){
//    return (a + b) * 2
//  }

const resultadoDoble = (numberA:number, numberB:number) => {
    return (numberA + numberB) * 2
}